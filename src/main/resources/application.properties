spring.application.name=hmt


# PostgreSQL Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/hmt
spring.datasource.username=postgres
spring.datasource.password=admin

# --- JPA & Hibernate Settings ---
# "update" tells Hibernate to automatically create/update tables
# based on your @Entity classes (like Patient.java)
spring.jpa.hibernate.ddl-auto=update

# This shows the SQL commands in your console, which is
spring.jpa.show-sql=true


# Redis togglee
otp.redis.uri=redis://localhost:6379

# OTP configs
otp.ttl.seconds=600
otp.max-attempts=5
otp.request-limit-per-hour=5
otp.lock-duration.seconds=900


jwt.secret=your_very_long_256_bit_secret_key_here_123456789012345678901234

# 5 minutes in ms (access token)
jwt.expiration.ms=300000
# 7 days
jwt.refresh.expiration.seconds=604800


# Refresh cookie settings
jwt.refresh.cookie-name=refresh
jwt.refresh.cookie-path=/
jwt.refresh.cookie-samesite=Strict



spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=hmtpriyanshu@gmail.com

spring.mail.password=tvug uxms vsub phiz
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true


SUPERADMIN_EMAIL=hmtpriyanshu@gmail.com
